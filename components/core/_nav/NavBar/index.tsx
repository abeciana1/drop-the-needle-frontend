import NavItem from '../NavItem'

const NavBar = () => {
    return (
        <nav>
            <NavItem
                text="Participate"
            />
        </nav>
    )
}

export default NavBar